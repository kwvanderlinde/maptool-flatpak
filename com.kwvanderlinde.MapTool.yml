id: com.kwvanderlinde.MapTool
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
command: /app/opt/maptool/bin/MapTool
modules:
  - name: MapTool
    buildsystem: simple
    build-commands:
      - mkdir /app/opt
      - cp -a maptool /app/opt/maptool
      - install -D MapTool.desktop /app/share/applications/${FLATPAK_ID}.desktop
      - install -D -m644 icon_256.png /app/share/icons/hicolor/256x256/apps/${FLATPAK_ID}.png
      - install -D -m644 metainfo.xml /app/share/metainfo/${FLATPAK_ID}.metainfo.xml
    sources:
      - type: archive
        dest-filename: maptool.zip
        dest: maptool
        url: https://github.com/RPTools/maptool/releases/download/1.18.6/MapTool-1.18.6-linux.zip
        sha256: 49da532f5f944099584a5ad5881e31e9c5536d3e65f359f97e5d53136d68d5e0
        only-arches:
          - x86_64
        strip-components: 0
      - type: file
        path: MapTool.desktop
      - type: file
        path: icon_256.png
      - type: file
        path: metainfo.xml
finish-args:
  # Required for --socket=x11
  - --share=ipc
  - --socket=x11
  #- --socket=fallback-x11
  #- --socket=wayland
  - --filesystem=home/.maptool-rptools
  # Mostly we just needs access to home/.maptool-rptools, except that libraries can be installed anywhere.
  - --filesystem=host
  - --device=dri
  - --share=network
